


<div class="o-wrapper u-pad-s">
  <p>
  
    <a href="/">Startseite</a>
  
  
    <a class="u-float-right" href="#menu">Unterseiten</a>
  </p>


  <a href="/{{pageId}}/"><h1 class="u-h1 u-color-1">{{page.title}}</h1></a>

  {{#> subtitle}}Artikel{{/subtitle}}

  {{#each links.articles }}
    {{> teaser}}
  {{/each}}


  <a name="menu"></a>
  {{#> subtitle}}Unterseiten{{/subtitle}}
  
  {{#each links.lists }}
    {{> link}}
  {{/each}}


  {{#if  debug}}
    <hr>
    <pre>{{{json}}}</pre>
  {{/if}}
  
  {{>button}}
    
</div>